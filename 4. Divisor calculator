dividend = int(input("Please enter a number to see its divisor: "))
divisor_list = []
for divisor in range(1, dividend + 1): #I added 1 to the dividend so it would include the dividend. For example if the the dividend was 8, the range only goes to 1 to 7. Adding 1 fixes that.
    qoutient = dividend % divisor
    if qoutient == 0:
        divisor_list.append(divisor)
print(divisor_list)
